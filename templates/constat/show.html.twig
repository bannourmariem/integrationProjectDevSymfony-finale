{% extends 'base.html.twig' %}

{% block title %}Constat{% endblock %}

{% block body %}
<div class="container mt-4">

    <div class="container mt-5 text-center">
        <h1 class="display-4 font-weight-bold text-primary">
            <span class="text-shadow"> Détails du Constat</span>
        </h1>
    </div>

    <ul class="list-group mt-3">
        <li class="list-group-item d-flex justify-content-between align-items-center">
            id:
            <span class="badge badge-primary badge-pill">{{ constat.id }}</span>
        </li>
      
        <li class="list-group-item d-flex justify-content-between align-items-center">
            Localisation:
            <span class="badge badge-primary badge-pill">{{ constat.localisation }}</span>
        </li>
       
        
        <li class="list-group-item d-flex justify-content-between align-items-center">
            Témoins:
            <span class="badge badge-primary badge-pill">{{ constat.temoins }}</span>
        </li>

        <li class="list-group-item d-flex justify-content-between align-items-center">
            Nom du preneur:
            <span class="badge badge-primary badge-pill">{{ constat.APreneurNom }}</span>
        </li>
        <li class="list-group-item d-flex justify-content-between align-items-center">
            Prénom du preneur:
            <span class="badge badge-primary badge-pill">{{ constat.APreneurPrenom }}</span>
        </li>
        <li class="list-group-item d-flex justify-content-between align-items-center">
            tel du preneur:
            <span class="badge badge-primary badge-pill">{{ constat.APreneurTel }}</span>
        </li>
       


        <li class="list-group-item d-flex justify-content-between align-items-center">
            A vehicule moteur marque:
            <span class="badge badge-primary badge-pill">{{ constat.AVehiculeMoteurMarque ? 'Oui' : 'Non' }}</span>
        </li>
        <li class="list-group-item d-flex justify-content-between align-items-center">
            A_vehicule_moteur_numImmatriculation:
            <span class="badge badge-primary badge-pill">{{ constat.AVehiculeMoteurNumImmatriculation }}</span>
        </li>
       
       


        <li class="list-group-item d-flex justify-content-between align-items-center">
            societe Assurance agence  nom:
            <span class="badge badge-primary badge-pill">{{ constat.ASocieteAssuranceAgenceNom }}</span>
        </li>
        
        <li class="list-group-item d-flex justify-content-between align-items-center">
            societe Assurance agence  adresse:
            <span class="badge badge-primary badge-pill">{{ constat.ASocieteAssuranceAgenceAdresse }}</span>
        </li>
       


        <li class="list-group-item d-flex justify-content-between align-items-center">
            Nom du preneur:
            <span class="badge badge-primary badge-pill">{{ constat.BPreneurNom }}</span>
        </li>
        <li class="list-group-item d-flex justify-content-between align-items-center">
            Prénom du preneur:
            <span class="badge badge-primary badge-pill">{{ constat.BPreneurPrenom }}</span>
        </li>
      
        <li class="list-group-item d-flex justify-content-between align-items-center">
            Téléphone du preneur:
            <span class="badge badge-primary badge-pill">{{ constat.BPreneurTel }}</span>
        </li>






        <li class="list-group-item d-flex justify-content-between align-items-center">
            A vehicule moteur marque:
            <span class="badge badge-primary badge-pill">{{ constat.BVehiculeMoteurMarque ? 'Oui' : 'Non' }}</span>
        </li>
        <li class="list-group-item d-flex justify-content-between align-items-center">
            A_vehicule_moteur_numImmatriculation:
            <span class="badge badge-primary badge-pill">{{ constat.BVehiculeMoteurNumImmatriculation }}</span>
        </li>
       


        
        <li class="list-group-item d-flex justify-content-between align-items-center">
            B_societeAssurance_agence_nom:
            <span class="badge badge-primary badge-pill">{{ constat.BSocieteAssuranceAgenceNom }}</span>
        </li>
        <li class="list-group-item d-flex justify-content-between align-items-center">
            B_societeAssurance_agence_adresse:
            <span class="badge badge-primary badge-pill">{{ constat.BSocieteAssuranceAgenceAdresse }}</span>
        </li>
        


        <li class="list-group-item d-flex justify-content-between align-items-center">
            B vehicule moteur marque:
            <span class="badge badge-primary badge-pill">{{ constat.BVehiculeMoteurMarque ? 'Oui' : 'Non' }}</span>
        </li>
        <li class="list-group-item d-flex justify-content-between align-items-center">
            B vehicule_moteur_numImmatriculation:
            <span class="badge badge-primary badge-pill">{{ constat.BVehiculeMoteurNumImmatriculation }}</span>
        </li>
        

        
    </ul>


</div>
    <div class="mt-3">
        <a href="{{ path('app_constat_index') }}" class="btn btn-secondary">Retour à la liste</a>
        <a href="{{ path('app_constat_edit', {'id': constat.id}) }}" class="btn btn-primary">Modifier</a>
        <a href="{{ path('app_constat_delete', {'id': constat.id}) }}" class="btn btn-primary">delete</a>

    </div>
{% endblock %} 
